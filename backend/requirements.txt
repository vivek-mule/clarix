# ── Core Framework ──────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-dotenv==1.1.0
pydantic==2.11.1
pydantic-settings==2.9.1

# ── Google Vertex AI (Gemini LLM) ──────────────────────────
google-cloud-aiplatform==1.86.0

# ── LangChain + LangGraph (Agent Orchestration) ────────────
langchain==0.3.23
langchain-core==0.3.51
langchain-google-vertexai==2.0.17
langchain-community==0.3.21
langgraph==0.3.30

# ── Embeddings (Local, no API needed) ──────────────────────
sentence-transformers==4.1.0          # all-MiniLM-L6-v2 — 384-dim

# ── Pinecone (Vector DB) ───────────────────────────────────
pinecone

# ── Supabase (Auth + PostgreSQL) ───────────────────────────
supabase==2.15.1

# ── HTTP / Async ────────────────────────────────────────────
httpx==0.28.1
aiofiles==24.1.0
sse-starlette==2.1.3

# ── Content Ingestion ───────────────────────────────────────
python-pptx==1.0.2                    # .pptx slide extraction
pypdf==4.2.0                          # .pdf text extraction for slide decks

# ── Utilities ───────────────────────────────────────────────
python-multipart==0.0.20              # file uploads in FastAPI
python-jose[cryptography]==3.4.0      # JWT token handling
passlib[bcrypt]==1.7.4                # password hashing
greenlet==3.0.3                       # avoid source build on py3.9 (Windows)
email-validator==2.2.0                # required for pydantic EmailStr
